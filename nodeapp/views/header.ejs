<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= appName %></title>
  <!-- Se incluyen los estilos de Bootstrap y Bootstrap Icons para mejorar el diseño -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
</head>
<body>
  <!-- Se agrega una barra de navegación responsiva usando componentes de Bootstrap -->
  <nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
      <a class="navbar-brand" href="/"><%= appName %></a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="/"><%= __('Home') %></a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/agents/new"><%= __('New Agent') %></a> 
            <!-- Agrega un enlace en la barra de navegación para acceder al formulario de creación de un nuevo agente -->
            <!--Se usa <\%= -->
          </li>
        </ul>

        
        <%# lang selector %>
        <div>
          <% getLocales().forEach( lang => { %>
            <%# alternativa con query string %>
            <a href="/change-locale/?locale=<%= lang %>"><%= lang %></a>
          <% }) %>
        </div>
        <%# login / logout %>

        <form class="d-flex" role="search">
          <% if (session.userId) { %>
            <!-- Muestra el botón de Logout si el usuario está autenticado -->
            <a href="/logout" class="btn btn-outline-success" type="submit">Logout</a>
          <% } else { %>
            <!-- Muestra el botón de Login si el usuario no está autenticado -->
            <a href="/login" class="btn btn-outline-success" type="submit">Login</a>
          <% } %>
        </form>
      </div>
    </div>
  </nav>