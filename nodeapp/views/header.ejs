<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= appName %></title>
  <!-- Bootstrap CSS -->

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <!--Se incluyen los estilos de Bootstrap y Bootstrap Icons para mejorar el diseño -->

  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
  <link rel="stylesheet" href="/main.css"> <!-- Se incluye el archivo de estilos principal -->


</head>
<body>
    <!-- Barra de navegación -->
  <!-- Se agrega una barra de navegación responsiva usando componentes de Bootstrap -->
  
  <nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
      <!-- Título o logo de la aplicación -->
      <a class="navbar-brand" href="/"><%= appName %></a>
      
      <!-- Botón de navegación para dispositivos móviles -->

      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <!-- Contenedor principal de la barra de navegación -->
      
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <!-- Menú principal alineado a la izquierda -->
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <!-- Enlace a Home -->
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="/"><%= __('Home') %></a>
          </li>
        <!-- Enlace a New Agent -->
          <li class="nav-item">
            <a class="nav-link" href="/agents/new"><%= __('New Agent') %></a> 
            <!-- Agrega un enlace en la barra de navegación para acceder al formulario de creación de un nuevo agente -->
            <!--Se usa <\%= -->
          </li>
        <!-- Menú desplegable de documentación API -->
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            <%= __('API Documentation') %>
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
            <li><a 
              class="dropdown-item" 
              href="/api-docs/swagger" 
              target="_blank" 
              rel="noopener noreferrer"
              ><%= __('Swagger') %></a></li>
            <li><a 
              class="dropdown-item" 
              href="/redoc-staticNode.html" 
              target="_blank" 
              rel="noopener noreferrer"
              ><%= __('Redoc') %></a></li>
          </ul>
        </li>
      </ul>
      <!-- Contenedor para los elementos alineados a la derecha -->
        <!-- ms-auto asegura que este contenedor esté completamente a la derecha -->
        <div class="d-flex align-items-center ms-auto">
          <!-- Selector de idiomas -->
          <!-- me-3 añade margen entre el selector de idiomas y el botón de login/logout -->
          <div class="lang-selector me-3">
            <% getLocales().forEach(lang => { %>
              <!-- Enlaces para cambiar el idioma con banderas -->
              <!-- me-2 añade margen entre las banderas -->
              <a href="/change-locale/?locale=<%= lang %>" class="me-2">
                <img src="/images/<%= lang %>.png" alt="<%= lang %>" style="width: 24px; height: auto;">
              </a>
            <% }) %>
          </div>

          <!-- Botón de login/logout -->
          <% if (session.userId) { %>
            <!-- Mostrar Logout si el usuario está autenticado -->
            <a href="/logout" class="btn btn-outline-success"><%= __('Logout') %></a>
          <% } else { %>
            <!-- Mostrar Login si el usuario no está autenticado -->
            <a href="/login" class="btn btn-outline-success"><%= __('Login') %></a>
          <% } %>
        </div>
      </div> <!-- Fin del contenedor principal del navbar -->
    </div> <!-- Fin del contenedor fluid del navbar -->
  </nav>

  <!-- Scripts de Bootstrap necesarios para funcionalidad (dropdown, colapsar menú, etc.) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-kenU1KFdBIe4zVF0s0G1M5b4hcpxyD9F7jL+rkE7Ckz4l2IMqEwA7lrnUqbt6L8N" crossorigin="anonymous"></script>
</body>
</html>    

      <!-- OPCION PROFE - SE MUEVE A LA DERECHA Y QUEDA MAL AL AÑADIR API DOC DESPLEGABLE PROPUESTO POR MI
        <%# lang selector %>
        <div class="lang-selector">
          <% getLocales().forEach( lang => { %>
            <%# alternativa con query string %>
            <a href="/change-locale/?locale=<%= lang %>">

              <img src="/images/<%= lang %>.png" alt="<%= lang %>">
            </a>
          <% }) %>
        </div>
        <%# login / logout %>

        <form class="d-flex" role="search">
          <% if (session.userId) { %>
            <!-- Muestra el botón de Logout si el usuario está autenticado 
            <a href="/logout" class="btn btn-outline-success" type="submit">Logout</a>
          <% } else { %>
            <!-- Muestra el botón de Login si el usuario no está autenticado 
            <a href="/login" class="btn btn-outline-success" type="submit">Login</a>
          <% } %>
        </form>
      </div>
    </div>
  </nav> --> 